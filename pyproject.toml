[project]
name = "SimpleImageSorter"
version = "0.1.0"
description = "To easily sort images into new folders"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyinstaller>=6.16.0",
    "pyqt6>=6.9.1",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
